CREATE TABLE First(
  code varchar(15),
  name varchar(50) NOT NULL,
  PRIMARY KEY (code)
);


CREATE TABLE Second(
  code varchar(15),
  name varchar(50) NOT NULL,
  PRIMARY KEY (code)
);



CREATE TABLE Third(
  code varchar(15),
  name varchar(50) NOT NULL,
  PRIMARY KEY (code)
);
